{
  "name": "farmershub-backend",
  "version": "1.1.0",
  "description": "FarmersHub backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node -e \"console.log('No build step required for FarmersHub backend')\"",
    "lint": "node -e \"console.log('No lint config found; skipping lint')\"",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}